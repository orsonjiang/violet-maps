{
  "name": "cypress",
  "scripts": {
    "client": "cd ../../client && npm ci && npm run test",
    "server": "cd ../../server && npm ci && npm start"
  },
  "devDependencies": {
    "cypress": "^13.4.0"
  }
}
